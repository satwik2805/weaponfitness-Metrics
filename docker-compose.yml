version: "3.8"

services:
  expo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weapon_fitness_expo
    ports:
      - "19000:19000"
      - "19001:19001"
      - "19002:19002"
    volumes:
      - .:/app
    command: ["npx", "expo", "start", "-c"]
